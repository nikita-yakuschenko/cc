export {
  normalizeUtmValue,
  normalizeCode,
  generateShortCode,
  defaultCodeLength,
  resolveCodeLength,
  isValidCodeLength,
  isReservedPath,
  isValidCustomAlias,
  buildPublicPath,
  buildShortUrl,
} from "@/lib/code";
